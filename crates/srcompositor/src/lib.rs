// srcompositor is now empty — all modules extracted to their own crates:
// blend → srblend, surface → srsurface, effects → sreffect, simd → srsimd
// This crate remains as a compatibility re-export or will be removed.
